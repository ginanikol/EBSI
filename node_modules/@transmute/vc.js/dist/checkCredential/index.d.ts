import { DocumentLoader } from "../types/DocumentLoader";
import CredentialCheck from "../types/CredentialCheck";
export declare const checkCredential: (credential: CredentialCheck, options: {
    documentLoader?: DocumentLoader;
    strict?: "ignore" | "warn" | "throw";
}) => Promise<undefined>;
