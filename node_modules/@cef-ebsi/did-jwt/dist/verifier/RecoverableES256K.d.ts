import { PublicKey } from "did-resolver";
import { IVerifier } from "../types";
export default class RecoverableES256KVerifier implements IVerifier {
    algoName: string;
    private static checkSignatureAgainstSigner;
    verify(data: string, signature: string, authenticators: PublicKey[]): PublicKey;
}
//# sourceMappingURL=RecoverableES256K.d.ts.map