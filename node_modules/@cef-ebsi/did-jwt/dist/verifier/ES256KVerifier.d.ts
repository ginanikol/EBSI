import { PublicKey } from "did-resolver";
import { IVerifier } from "../types";
export default class ES256KVerifier implements IVerifier {
    algoName: string;
    verify(data: string, signature: string, authenticators: PublicKey[]): PublicKey;
}
//# sourceMappingURL=ES256KVerifier.d.ts.map