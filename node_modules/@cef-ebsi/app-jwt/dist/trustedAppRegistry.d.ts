declare class TrustedAppRegistry {
    provider: string;
    constructor(provider: string);
    getPublicKey(appName: string): Promise<string | null>;
    checkAuthorization(appName: string, authorizedAppName: string): Promise<boolean>;
    verify(token: string): Promise<any>;
}
export default TrustedAppRegistry;
//# sourceMappingURL=trustedAppRegistry.d.ts.map