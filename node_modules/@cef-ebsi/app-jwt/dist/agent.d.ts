import jose from "jose";
import { SimpleSigner } from "@cef-ebsi/did-jwt";
interface User {
    publicKey: string;
    did: string;
    signer: ReturnType<typeof SimpleSigner>;
}
declare class Agent {
    issuer: string;
    privateKey: string | jose.JWK.ECKey;
    scope: string;
    user: undefined | User;
    expiration: {
        requestToken: number;
        accessToken?: number;
    };
    constructor(scope?: string, privateKey?: string | jose.JWK.ECKey, opts?: {
        expiration?: {
            requestToken: number;
        };
        issuer?: string;
    });
    createRequestPayload(appName: string, opts?: {
        [key: string]: string | number;
    }): Promise<any>;
}
export default Agent;
//# sourceMappingURL=agent.d.ts.map