import * as jwkJcsPubCodec from "./codecs/jwk_jcs-pub.js";
import { KEY_DID_METHOD_PREFIX } from "./constants.js";
import { encodePublicKey, resolveDidDoc } from "./internals.js";
export function createDid(publicKey) {
    try {
        return `${KEY_DID_METHOD_PREFIX}${encodePublicKey(jwkJcsPubCodec.encode(publicKey), jwkJcsPubCodec.code)}`;
    }
    catch (e) {
        throw new Error(e instanceof Error ? e.message : "Unknown error");
    }
}
export function validateDid(did) {
    resolveDidDoc(did);
}
//# sourceMappingURL=util.js.map